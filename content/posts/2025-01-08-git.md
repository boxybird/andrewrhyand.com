---
title: "Git"
date: 2025-01-08
excerpt: "A quick reference for the commands I use daily."
---

# Git

The commands I actually use:

```bash
git status
git add -p
git commit -m "message"
git push
git pull --rebase
```

That's it. That's the post.
